from setuptools import setup

setup(
   name='searchValues',
   version='0.1.0',
   author='G LaLonde',
   author_email='lalondeg1@michigan.gov',
   description='Search a folder of csv files!',
   install_requires=[
       "pandas >= 1.2.1",
   ],
)